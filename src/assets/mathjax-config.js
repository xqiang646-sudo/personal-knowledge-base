window.MathJax = {
  output: {
    // MathJax 4 enables browser-driven breaks inside inline equations by
    // default. For teaching prose, moving a whole short formula to the next
    // line is easier to read than splitting it after an equals sign.
    linebreaks: {
      inline: false,
      width: "100%"
    }
  }
};
